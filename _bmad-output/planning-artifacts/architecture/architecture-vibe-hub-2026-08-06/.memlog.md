---
scope: Vibe Hub v1 architecture spine (all PRD features/FRs, altitude: feature/above-epics)
purpose: build-substrate
altitude: feature
updated: 2026-08-06T10:24
---

- (decision) Working mode: Coaching path (user chose it explicitly over Fast path)
- (decision) Deliverable scope: spine only (ARCHITECTURE-SPINE.md), no fuller design doc or deck requested
- (decision) Primary input: prd-vibe-hub-2026-08-05/prd.md (status: final), all 9 FRs across 4 features (Landing, Course Catalog, IA Chat, Tools Grid/UI Builder). No prior spine exists to inherit from.
- (assumption) Codebase reality-check (brownfield, no project-context.md exists) via direct read of App.jsx, WorkspacePage.jsx, AdminPage.jsx, OutilsTab.jsx, IATab.jsx, services/supabase.js, services/ai.js, vite.config.js, package.json
- (assumption) CRITICAL GAP vs PRD: FR-7 UI Builder is NOT wired to Gemini today. OutilsTab.jsx UIBuilderView() is a hardcoded setTimeout(2500ms) mock with static fake output panel -- no call to services/ai.js or Gemini, no real code generation/rendering. PRD text assumed this already works via Gemini; it does not. This is unbuilt v1 scope, not a documentation gap.
- (assumption) FR-4 Admin gate is further along than PRD assumed: AdminPage.jsx already implements full Supabase Auth (supabase.auth.signInWithPassword, session-based, onAuthStateChange), not a 'single shared credential'. Enforcement point (RLS policies on courses table) is NOT visible in the repo -- no migrations/schema files exist; Supabase schema is presumably managed via dashboard, unverifiable from code alone.
- (assumption) IATab.jsx sessions/projects/skills persist to localStorage only, keyed globally (no per-user namespace) -- matches FR-3's 'session-local' framing for course progress, but the PRD does not explicitly scope IA-tab chat history the same way; worth confirming as the same v1 pattern.
- (assumption) FR-9 waitlist-count view does not exist anywhere in codebase (confirmed: AdminPage.jsx has no waitlist section). services/supabase.js has no aggregation query (e.g. count-by-tool_id) for the waitlist table. Confirmed net-new build.
- (assumption) No router: App.jsx toggles 'landing'/'app' view via useState; /admin is matched via raw window.location.pathname check, bypassing React tree entirely. Matches PRD 6.2 Out-of-Scope (no React Router in v1).
- (event) Reality-check complete, entering Coaching-path elicitation with user on paradigm/boundaries before drafting AD's
- (decision) AD candidate: Gemini calls (IA chat FR-5 + UI Builder FR-7) move server-side behind a Supabase Edge Function proxy; VITE_GEMINI_API_KEY never ships to the browser. User chose this over keeping it client-side, citing PRD's own rising-cost-risk note.
- (decision) AD candidate: FR-7 UI Builder 'rendered' output means a live in-browser sandboxed preview of Gemini's generated code (not just formatted/copyable text), matching the product's own UI-quality pitch (PRD SS1). Needs a sandboxed React runtime library -- to be verified/chosen (react-live vs Sandpack vs iframe+esm).
- (decision) AD candidate: FR-4 write enforcement is Postgres RLS on the courses (and waitlist) tables, tied to Supabase Auth -- anon/public role read-only (published courses only; waitlist insert-only), only authenticated admin role can write. Client-side session check in AdminPage.jsx is UX only, not the security boundary.
- (decision) Deployment target: static SPA host (Vercel or Netlify) for the Vite/React app; Supabase Edge Function (Deno runtime) hosts the Gemini proxy alongside the existing Supabase project. No specific host chosen between Vercel/Netlify -- user deferred, treat as open/interchangeable.
- (version) Web-verified: Sandpack (CodeSandbox) will no longer be actively maintained as of March 2026 -- do not adopt for FR-7 live preview. react-live's latest release (4.1.8) is ~2 years stale. Neither is a safe pick for a new dependency in 2026.
- (decision) AD candidate: given Sandpack/react-live maintenance risk, FR-7 live preview uses a minimal self-built sandbox: iframe with srcdoc + in-browser Babel standalone transform of the Gemini-generated JSX, never eval'd/rendered directly in the app's own DOM/React tree (LLM output is untrusted input -- this is a security invariant, not just an implementation choice). Exact transform tooling left to implementation; the iframe-isolation rule is the binding part.
- (decision) AD candidate: services/*.js are the only modules allowed to import '@supabase/supabase-js' or '@google/generative-ai' directly. AdminPage.jsx currently imports the raw supabase client for auth calls -- to be closed by adding signIn/signOut/getSession/onAuthStateChange wrappers to services/supabase.js so components never reach around the service layer, including for auth.
- (decision) AD candidate: two-tier state/persistence convention -- durable/shared data (courses, waitlist) lives in Supabase via services/, always; ephemeral per-browser UI state (course progress FR-3, IA sessions/projects/skills) lives in localStorage under feature-prefixed keys, never a third mechanism (sessionStorage/IndexedDB/global store) without amending this rule. No global state manager (Redux/Zustand/Context-as-store) introduced -- component-local useState stays the pattern, matching existing code.
- (decision) AD candidate: FR-9 waitlist counts are served by a Postgres view or RPC function (waitlist_counts: tool_id, count) restricted to the admin role via RLS, exposed through a new services/supabase.js getWaitlistCounts() -- not raw client-side aggregation of full waitlist rows (would require granting admin read of raw emails unnecessarily).
- (decision) Paradigm: thin layered architecture -- Presentation (pages/, components/) -> Service Adapters (services/*.js, sole gateway to external systems) -> External Systems (Supabase Postgres+Auth, Supabase Edge Functions, Gemini). No framework-level state manager, no formal DI container -- deliberately minimal given single-builder/small-team stakes.
- (version) Web-verified: Supabase Edge Functions run Deno 2.x (2.1.4) as of 2026, upgraded from Deno 1.45.2 -- current recommended runtime for the Gemini proxy function.
- (event) Coaching elicitation complete on load-bearing forks (Gemini key exposure, UI Builder render fidelity, write enforcement, deployment target, service-layer boundary, state/persistence convention, waitlist-count mechanism, sandbox isolation). Proceeding to distill spine.
- (event) Spine drafted (v1). lint_spine.py clean (0 findings). Dispatched 2 configured finalize_reviewers as parallel subagents: tech-verification and adversarial-consistency.
- (event) Adversarial reviewer returned: 2 high findings (AD-3/AD-4 split lets FR-8 implementer grant admin SELECT on raw waitlist table, defeating AD-4's narrower-grant intent; AD-6 pins storage location but not shape, letting FR-5/FR-6 diverge on conversation<->project cross-reference direction), plus medium (view vs SECURITY DEFINER RPC have different RLS semantics, AD-4 treats them as interchangeable), low-medium (error-shape convention only forbids mixing within one function, not across a file), low (order_index vs created_at canonical-ordering ambiguity for admin reordering). Full review at reviews/review-adversarial.md. Awaiting tech-verification reviewer before applying fixes.
- (event) Tech-verification reviewer returned: @google/generative-ai 0.24.1 is deprecated by Google (superseded by @google/genai v2.15.0) -- material since AD-1 requires new Edge Function code targeting Gemini right now, should use the current SDK not the deprecated one. Deno 2.1.4 claim is accurate as Supabase's pinned version but wording overstated it as 'current' vs Deno's own mainline (2.9.x by mid-2026). Vite 5.4.0 and Tailwind 3.4.7 flagged as informational version-debt, not blocking (build-time tool / not architecturally load-bearing). Sandpack/react-live claims confirmed accurate, no better-maintained named replacement found -- deferral to implementation time is correct.
- (decision) Triage of both reviewers (applying now, both high-severity adversarial findings + the material tech finding, plus the smaller ones cheap to fix): (1) tighten AD-3 to explicitly forbid admin SELECT grant on raw waitlist table (only via AD-4's view) -- closes FR-8/AD-3-vs-AD-4 gap. (2) AD-4: pin waitlist_counts as a plain security-invoker view, not SECURITY DEFINER RPC, to avoid RLS-bypass ambiguity. (3) new consistency-convention row fixing IA session<->project reference direction (session holds projectId, ratifying existing IATab.jsx shape) to close FR-5/FR-6 divergence risk. (4) tighten error-shape convention scope from 'within one function' (vacuous) to a real per-operation-type rule. (5) fix order_index vs created_at ambiguity -- order_index is canonical everywhere, ratifying getCourses' existing behavior. (6) Stack table: @google/generative-ai marked deprecated, Edge Function targets @google/genai v2.15.0 instead. (7) Deno wording corrected to 'Supabase's pinned Edge Runtime version' not 'current'. (8) Vite/Tailwind version debt added to Deferred as non-blocking notes.
- (event) Applied all reviewer fixes to ARCHITECTURE-SPINE.md: AD-3 tightened (no admin SELECT on raw waitlist table), AD-4 pinned to security-invoker view not SECURITY DEFINER RPC, new consistency-convention row for IA session->project reference direction, error-shape convention scope tightened, order_index ratified as sole canonical ordering, @google/generative-ai flagged deprecated with @google/genai v2.15.0 as the Edge Function target, Deno wording corrected to 'Supabase's pinned version', Vite/Tailwind version debt added to Deferred as non-blocking. Re-ran lint_spine.py: 0 findings. Spine finalized, status: final, updated: 2026-08-06.
